 {/* <motion.div className="box"
             drag
            animate={{
                x:work?1000:0,
                opacity:1,
                rotate:work?360:0,
                
            }}
            initial={{
                opacity:0.2,
                backgroundColor:"aqua"
            }}
            transition={{
                type:'spring',
                stiffness:200,
                damping:60,
                backgroundColor:"red"
            }}
            whileHover={{
                backgroundColor:"red",
                scale: 1.1
            }}
            whileTap={{
                scale:0.8
            }}
            onClick={()=> setWork(work?false:true)}
            >
           <h1>Stack</h1>
            </motion.div> */}